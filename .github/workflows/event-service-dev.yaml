name: CI-event-service
on:
    push:
        branches:
            - main
            - dev
            - feat/**
            - fix/**
        paths:
            - "apps/event-service/**"
            - ".github/workflows/event-service-dev.yaml"
            - ".github/workflows/test-reusable.yaml"
            - ".github/workflows/lint-reusable.yaml"
            - ".github/workflows/build-reusable.yaml"
            - "packages/**"
    pull_request:
        branches:
            - main
            - dev
permissions:
    contents: read
concurrency:
    group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.event.pull_request.number || github.ref }}
    cancel-in-progress: ${{ github.event_name == 'pull_request' || github.ref != 'refs/heads/main' }}
jobs:
    event-service-lint:
        uses: ./.github/workflows/lint-reusable.yaml
        with:
            service-name: "event-service"
            node-version: "20"
            pnpm-version: "10.26.1"
    event-service-build:
        uses: ./.github/workflows/build-reusable.yaml
        with:
            service-name: "event-service"
            node-version: "20"
            pnpm-version: "10.26.1"
    event-service-test:
        needs: [event-service-lint, event-service-build]
        strategy:
            matrix:
                node-version: ["20", "22"]
        uses: ./.github/workflows/test-reusable.yaml
        with:
            service-name: "event-service"
            node-version: "${{ matrix.node-version }}"
            pnpm-version: "10.26.1"
